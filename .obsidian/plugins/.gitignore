# .obsidian/plugins/.gitignore
# Track plugin configurations for reproducibility while protecting secrets

# ============================================================================
# Plugin Code: Now TRACKED for persistence across clones/pulls
# ============================================================================
# Previously ignored *.js and *.css but this caused plugins to not persist
# after git operations. Now tracking them for reliability.
# 
# NOTE: This increases repo size but ensures plugins work immediately.
# ============================================================================
*.exe

# ============================================================================
# ALWAYS IGNORE: Cache Files
# ============================================================================
**/cache.json
**/cache/

# ============================================================================
# ALWAYS IGNORE: Git Plugin Script
# ============================================================================
obsidian-git/obsidian_askpass.sh

# ============================================================================
# PLUGINS WITH SECRETS: Explicitly ignore data.json for these plugins
# ============================================================================
# These plugins store API keys, tokens, or cryptographic material in data.json
# Create a data.json.example file in each to document the structure

# copilot - stores OpenAI/Anthropic/Google/etc API keys
copilot/data.json

# obsidian-local-rest-api - stores API key, private key, and certificates
obsidian-local-rest-api/data.json

# obsidian-imgur-plugin - stores Imgur client ID and album IDs
obsidian-imgur-plugin/data.json

# smart-connections - stores API keys for AI services
smart-connections/data.json

# readwise-official - stores Readwise API token
readwise-official/data.json

# obsidian-raindrop-highlights - stores Raindrop API token
obsidian-raindrop-highlights/data.json

# github-copilot - stores GitHub auth tokens
github-copilot/data.json

# google-drive-sync - stores OAuth tokens
google-drive-sync/data.json

# obsidian-google-lookup - stores Google API key
obsidian-google-lookup/data.json

# medium-importer
medium-importer/data.json

# raindrop
obsidian-raindrop/data.json

# ============================================================================
# TRACKED BY DEFAULT: All other data.json files are tracked
# ============================================================================
# Safe configurations that should be version controlled:
# - templater-obsidian/data.json (folder templates)
# - obsidian-git/data.json (commit messages, preferences)
# - todoist-sync-plugin/data.json (UI preferences, token stored separately)
# - calendar/data.json (UI preferences)
# - terminal/data.json (terminal profiles)
# - metadata-menu/data.json (field definitions)
# - lazy-plugins/data.json (plugin load order)
# - obsidian-plugin-groups/data.json (plugin groups)
# - etc.
